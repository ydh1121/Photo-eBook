# Photo-eBook UI Regression Spec

이 문서는 이미 사용자 확인을 통과한 UI 동작과 시각 규칙을 고정하기 위한 회귀 방지 명세다. 이후 수정은 해당 요청 범위만 변경하고, 아래 항목은 명시적인 요청이 없는 한 유지한다.

## 1. 상단 챕터 필터

- `.nav-shell`은 라이트/다크 모두 **투명**이다. 상단 필터 레일 뒤에 별도의 대지색, 검정판, 반투명 패널을 추가하지 않는다.
- 실제 유리 재질은 `.nav-scroll` 레일과 moving liquid indicator가 담당한다.
- 상단 레일의 좌우 스크롤은 Safari/WebKit 네이티브 `overflow-x:auto`가 소유한다.
- JS에서 `scrollLeft`, `scrollIntoView`, pointer/touch move 기반 커스텀 패닝으로 상단 레일을 강제 이동하지 않는다.
- 필터칩 탄성은 Breeze 계열 easing을 유지한다. 현재 승인된 이동감은 임의로 변경하지 않는다.
- 페이지 읽기 진행도는 `.nav-scroll`의 `--v32-progress`로 표현한다. 프로그레스 복구를 위해 별도 배경판을 만들지 않는다.
- 첫 번째 필터로 복귀할 때 탄성 오버슈트가 왼쪽 경계에서 잘리지 않도록 현재 runway/padding을 유지한다.

## 2. 브라우저 하단 크롬 / 페이지 대지

- Safari 하단 주소 알약 뒤에 사이트가 만든 별도 색상판이 보이면 안 된다.
- 이를 위해 `html`, `body`의 브라우저-facing 배경은 투명하게 유지한다.
- 반대로 `#app`, `.app`, 실제 section까지 투명하게 만들면 안 된다. 앱 대지는 테마의 정상 색을 유지한다.
- 다크 앱 대지 기준값은 `#0d0f13` 계열이며 Safari/WebKit의 순수 검정 바닥이 노출되면 회귀다.
- 하단 플로팅 버튼의 그림자는 safe-area 또는 브라우저 크롬에 의해 잘리면 안 된다.

## 3. Liquid Glass 필터 공통

- 레일은 뒤쪽 재질, moving indicator는 선택 상태, 버튼 텍스트는 최상위의 3단 위계를 유지한다.
- moving indicator가 존재할 때 active 버튼 자체에 두 번째 파란 배경을 그리지 않는다.
- 필터 전환은 한 개의 moving indicator만 보여야 한다. 목적지에 새 파란 칩이 먼저 생기거나 두 칩이 겹치면 회귀다.
- 구형 indicator/legacy segment가 재생성되어도 화면에는 하나만 보여야 한다.

## 4. 내 모음 1차 탭

- `전체 / 영상 / 읽을거리 / 질문 / 설정`은 내 모음의 **1차 내비게이션**이다.
- 화면 폭을 적극적으로 사용하며 2차 필터보다 시각적으로 우선한다.
- 검색창은 질문 탭에서도 사라지지 않는다.

## 5. 질문 2차 필터

- `질문 작성 / 저장한 질문`은 질문 탭 내부의 **2차 필터**다. 1차 내 모음 탭보다 작고 덜 지배적이어야 한다.
- 레일은 화면 전체 폭을 차지하지 않는다. 현재 기준 최대 폭은 약 `224px`.
- 레일 높이 기준은 `52px`, 실제 선택 liquid pill 높이는 `42px`.
- 두 버튼은 동일한 열 너비를 사용한다.
- `저장한 질문`의 숫자는 별도 CTA가 아니라 **3차 메타데이터 badge**다. 과한 파란 원/강한 대비를 쓰지 않는다.
- inactive badge는 낮은 대비의 blue-grey, active badge는 선택 pill 안에서 은은한 white translucent 처리를 사용한다.
- 질문 2차 필터가 두 개 보이거나 legacy 질문 필터가 같이 나타나면 회귀다.

## 6. 질문 작성 패널

- `#askQuote`의 빈 상태 문구 `문장을 선택하면 여기에 표시됩니다.`는 quote 영역의 세로 중앙에 위치한다.
- quote 영역은 한 줄일 때 중앙 정렬하고, 실제 선택 문장이 여러 줄이면 내용에 맞게 자연스럽게 높이가 늘어난다.
- 질문 textarea와 quote 영역은 글자 크기/패딩/모서리 비율이 주변 UI보다 과도하게 크거나 작지 않아야 한다.
- `프롬프트 복사 / 질문 저장 / ChatGPT 열기`의 동작과 배치는 다른 스타일 수정 때문에 깨지면 안 된다.

## 7. 선택 모드

- 선택 모드의 라디오 버튼은 하위 필터 전환/목록 재렌더 후에도 유지한다.
- 선택 원형 / 썸네일 또는 Q / 본문은 별도 열을 사용한다. 라디오 버튼을 만들기 위해 padding으로 기존 콘텐츠를 억지로 밀지 않는다.
- 선택 모드 진입으로 카드 높이가 비정상적으로 증가하거나 텍스트가 오른쪽으로 밀리면 회귀다.

## 변경 규칙

1. 이미 통과한 항목은 요청 없이 재설계하지 않는다.
2. 한 버그를 고칠 때 관련 없는 전역 `html/body/app/nav-shell` 스타일을 함께 변경하지 않는다.
3. 새 override를 추가하기 전에 기존 최종 override와 충돌 여부를 확인한다.
4. 동일 기능의 상태/indicator/scroll owner는 하나만 둔다.
5. 캐시 버전을 올릴 때 실제 `index.html` 로딩 경로도 함께 확인한다.
