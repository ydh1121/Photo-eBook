# Photo-eBook UI Regression Spec

이 문서는 이미 사용자 확인을 통과한 UI 동작과 시각 규칙을 고정하기 위한 회귀 방지 명세다. 이후 수정은 해당 요청 범위만 변경하고, 아래 항목은 명시적인 요청이 없는 한 유지한다.

## 1. 상단 챕터 필터

- `.nav-shell`은 라이트/다크 모두 시각적으로 **투명**이다. 상단 필터 레일 뒤에 별도의 대지색, 검정판, 반투명 패널을 추가하지 않는다.
- 실제 유리 재질은 `.nav-scroll` 레일과 moving liquid indicator가 담당한다.
- 상단 레일의 좌우 스크롤은 Safari/WebKit 네이티브 `overflow-x:auto`가 소유한다.
- JS에서 `scrollLeft`, `scrollIntoView`, pointer/touch move 기반 커스텀 패닝으로 상단 레일을 강제 이동하지 않는다.
- 필터칩 탄성은 Breeze 계열 easing을 유지한다. 현재 승인된 이동감은 임의로 변경하지 않는다.
- 페이지 읽기 진행도는 `.nav-scroll`의 `--v32-progress`로 표현한다. 프로그레스 복구를 위해 별도 배경판을 만들지 않는다.
- 첫 번째 필터로 복귀할 때 탄성 오버슈트가 왼쪽 경계에서 잘리지 않도록 현재 runway/padding을 유지한다.
- PC에서는 필터 레일을 뷰포트 폭으로 늘리지 않는다. 실제 칩 묶음의 폭을 기준으로 한 유리 캡슐 하나가 화면 중앙에 놓여야 한다.

## 2. iPhone Safari 브라우저 크롬 / 페이지 대지

- 2026-08-19 iPhone 실기기 검증에서 승인된 해결책을 기준선으로 사용한다.
- iOS WebKit에서 최초 접속 시 `html`, `body`는 실제 테마 canvas를 가진다. 라이트는 `#fff`, 다크는 `#0d0f13` 계열이다.
- iOS Safari 최초 expanded 주소영역 상태에서는 `.nav-shell`을 `position:relative`로 시작한다. 최초부터 `sticky`를 강제하면 주소영역 뒤 solid extension이 재발할 수 있다.
- Safari browser chrome이 compact된 것이 `visualViewport` 증가 또는 충분한 scroll로 확인되면 `<html>`에 `safari-nav-sticky-armed`를 추가하고 기존 `position:sticky; top:0` 동작으로 전환한다.
- `#app`, `.app`, 실제 section은 정상 테마 대지를 유지한다. 다크 앱 대지 기준값은 `#0d0f13` 계열이며 순수 검정 바닥이 노출되면 회귀다.
- 최초 주소영역 문제를 고치기 위해 collection/FAB/question popup을 숨기거나 삭제하지 않는다. 하단 UI는 원인 분리 결과 직접 원인이 아니었다.
- 과거처럼 collection을 보이지 않게 열고 탭을 바꿨다가 닫는 Safari prime workaround를 다시 추가하지 않는다.
- `top:5px` 같은 임의 edge offset으로 sticky를 유지하는 우회도 사용하지 않는다.
- 하단 플로팅 버튼의 그림자는 safe-area 또는 브라우저 크롬에 의해 잘리면 안 된다.
- Safari 전용 root/nav 규칙은 `ios-webkit-chrome` 범위 밖 브라우저의 geometry를 변경하면 안 된다.

## 3. Liquid Glass 필터 공통

- 레일은 뒤쪽 재질, moving indicator는 선택 상태, 버튼 텍스트는 최상위의 3단 위계를 유지한다.
- moving indicator가 존재할 때 active 버튼 자체에 두 번째 파란 배경을 그리지 않는다.
- 필터 전환은 한 개의 moving indicator만 보여야 한다. 목적지에 새 파란 칩이 먼저 생기거나 두 칩이 겹치면 회귀다.
- 구형 indicator/legacy segment가 재생성되어도 화면에는 하나만 보여야 한다.

## 4. 내 모음 1차 탭

- `전체 / 영상 / 읽을거리 / 질문 / 설정`은 내 모음의 **1차 내비게이션**이다.
- 화면 폭을 적극적으로 사용하며 2차 필터보다 시각적으로 우선한다.
- 검색창은 질문 탭에서도 사라지지 않는다.

## 5. 질문 2차 필터

- `질문 작성 / 저장한 질문`은 질문 탭 내부의 **2차 필터**다.
- `#v40QuestionControls`는 `#collectionTools` 안에 위치한다. `#collectionTools`가 이미 좌우 content gutter를 가지고 있으므로 질문 2차 필터 자체에 별도의 좌우 padding을 추가하지 않는다.
- 바깥 레일의 **왼쪽/오른쪽 끝은 `.collection-search`와 정확히 일치**해야 한다. 검색창보다 안쪽으로 한 번 더 들어가거나 독립된 좁은 캡슐처럼 보이면 회귀다.
- 두 목적지는 정확히 동일한 `1/2` 인터랙션 슬롯을 가진다. 버튼 자체는 각 슬롯 전체를 사용해 클릭영역과 라벨 중심을 고정한다.
- 선택 liquid pill은 각 슬롯 전체를 채우지 않고, 각 슬롯 중앙에 놓인 동일한 시각 폭을 사용한다. 현재 기준은 슬롯 폭에서 약 `18px`를 뺀 값, 최대 `154px`이며 360px 이하에서는 약 `14px`를 빼고 최대 `146px`다.
- 왼쪽 선택과 오른쪽 선택의 **pill 폭은 완전히 동일**해야 하고, pill의 중심은 각각 왼쪽/오른쪽 슬롯 중심과 일치해야 한다.
- 왼쪽 선택 시 레일 왼쪽 여백과 오른쪽 선택 시 레일 오른쪽 여백은 서로 거울처럼 대칭이어야 한다. 한쪽만 가장자리에 붙거나 다른 쪽만 안쪽으로 밀리면 회귀다.
- 버튼 라벨 문자열 자체가 각 슬롯의 정확한 X축 중앙에 위치해야 한다. `저장한 질문` 뒤의 숫자 badge가 라벨을 왼쪽으로 밀면 안 된다.
- 숫자 badge는 라벨 중앙 정렬과 독립된 3차 메타데이터다. 비활성 상태에서는 낮은 대비의 neutral blue-grey, active 상태에서는 선택 pill 안에서 은은한 white translucent 처리를 사용한다.
- 1차/2차 위계는 너비 차이로 만들지 않는다. 2차 필터는 더 낮은 높이, 작은 글자, 약한 재질 대비로 위계를 만든다.
- 현재 기준은 바깥 레일 높이 약 `50px`, 실제 선택 liquid pill 높이 약 `40px`다.
- 질문 2차 필터가 두 개 보이거나 legacy 질문 필터가 같이 나타나면 회귀다.

## 6. 질문 작성 패널

- `#askQuote`의 빈 상태 문구 `문장을 선택하면 여기에 표시됩니다.`는 quote 영역의 세로 중앙에 위치한다.
- quote 영역은 한 줄일 때 중앙 정렬하고, 실제 선택 문장이 여러 줄이면 내용에 맞게 자연스럽게 높이가 늘어난다.
- 질문 textarea와 quote 영역은 글자 크기/패딩/모서리 비율이 주변 UI보다 과도하게 크거나 작지 않아야 한다.
- `프롬프트 복사 / 질문 저장 / ChatGPT 열기`의 동작과 배치는 다른 스타일 수정 때문에 깨지면 안 된다.

## 7. 선택 모드

- 선택 모드의 라디오 버튼은 하위 필터 전환/목록 재렌더 후에도 유지한다.
- 선택 원형 / 썸네일 또는 Q / 본문은 별도 열을 사용한다. 라디오 버튼을 만들기 위해 padding으로 기존 콘텐츠를 억지로 밀지 않는다.
- 선택 모드 진입으로 카드 높이가 비정상적으로 증가하거나 텍스트가 오른쪽으로 밀리면 회귀다.

## 8. 모바일 승인 기준선 — 시각 변경 금지

- 2026-08-18 기준 모바일 화면과 2026-08-19 승인된 iPhone Safari chrome 수정은 사용자 확인을 통과한 **승인 기준선**이다.
- 휴대폰 구간의 레이아웃, 간격, 글자 크기, 이미지 비율, 카드 크기, 챕터 순서, 상단 필터 시각 위치, 내 모음 geometry, 질문 UI geometry, safe-area 처리는 명시적인 사용자 요청 없이는 변경하지 않는다.
- iPhone Safari의 최초 nav `relative → compact 후 sticky` lifecycle은 geometry 버그 수정이므로 승인 기준에 포함한다.
- PC 개선을 위한 시각 CSS는 원칙적으로 `@media (min-width:1024px)` 안에만 작성한다. 데스크톱 문제를 고친다는 이유로 기존 모바일 기본값을 바꾸지 않는다.
- 모바일의 가로 레일은 지금 승인된 네이티브 터치/스와이프 동작을 그대로 유지한다. PC용 wheel 또는 mouse-drag 보강 로직은 모바일 pointer/touch 동작을 가로채면 안 된다.
- 기능 오류를 고치기 위한 공통 JS 변경은 허용하지만, 그 변경으로 모바일의 위치·크기·여백·표면 디자인이 달라지면 회귀다.
- `다른 기기에서 이어보기`, 저장/동기화, 외부 링크 열기처럼 기기 공통 기능은 모바일/PC 모두 동작해야 하며, 기능 수정과 모바일 시각 재설계는 별개로 취급한다.
- 모바일 시각 기준선을 변경해야 하는 작업이 생기면 먼저 사용자에게 명시적으로 승인받는다.

## 9. PC 가로 레일

- PC의 가로 카드/이미지 레일은 **마우스 왼쪽 버튼으로 잡아 끌 때만** 가로 이동한다. 세로 휠이나 트랙패드 제스처가 가로 이동을 소유하거나 세로 페이지 스크롤 속도를 떨어뜨리면 회귀다.
- 레일 안의 링크, 이미지, 텍스트는 드래그 중 네이티브 drag ghost나 텍스트 선택을 시작하지 않는다. 실제 드래그 뒤의 click도 실행하지 않는다.
- 첫 번째 카드의 왼쪽 면은 일반 본문 텍스트 시작선과 맞춘다. 다만 카드 그림자까지 잘리면 안 되므로 실제 클리핑 경계와 카드 사이에는 충분한 shadow runway를 둔다.
- 왼쪽 가장자리는 별도 fade/blur 없이 하드 클립한다.
- 오른쪽 가장자리는 배경색으로 막지 않는다. 반투명 `backdrop-filter` 기반의 짧은 blur/fade 영역을 두어 다음 카드가 이어짐을 보여준다.
- 마지막 카드는 끝까지 끌었을 때 오른쪽 blur 아래에 내용이 가려진 채 멈추지 않도록 충분한 trailing runway를 둔다.
- 실무 카드처럼 비동기로 추가되거나 재렌더되는 레일도 동일한 마우스 드래그 동작과 카드 내부 grid 안정성을 유지해야 한다.

## 변경 규칙

1. 이미 통과한 항목은 요청 없이 재설계하지 않는다.
2. 한 버그를 고칠 때 관련 없는 전역 `html/body/app/nav-shell` 스타일을 함께 변경하지 않는다. 단, `ios-webkit-chrome`에 scope된 승인 Safari root/nav 규칙은 유지한다.
3. 새 override를 추가하기 전에 기존 최종 override와 충돌 여부를 확인한다.
4. 동일 기능의 상태/indicator/scroll owner는 하나만 둔다.
5. 캐시 버전을 올릴 때 실제 `index.html` 로딩 경로도 함께 확인한다.
6. runtime 파일을 이동/rename할 때 CSS/JS 로드 순서를 그대로 유지하고 동적 import/postload 경로까지 함께 검증한다.
