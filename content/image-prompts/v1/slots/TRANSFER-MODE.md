# Binary transfer mode

Final production image transport is direct Git blob creation whenever the connector can reproduce the exact expected Git blob SHA. Text staging is legacy recovery only and must not become the normal image curriculum.
